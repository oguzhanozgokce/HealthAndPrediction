package com.oguzhanozgokce.healthandprediction.data.repos


class CardiovascularRepo {
    /**private lateinit var tflite: Interpreter

    fun sendUserInputToModel(userInput: UserInput): FloatArray {
        val inputArray = convertUserInputToTensorFlowFormat(userInput)
        val outputArray = Array(1) { FloatArray(1) }
        tflite.run(inputArray, outputArray)
        // outputArray now contains the results from the model

        // Return the result
        return outputArray[0]
    }
    private fun convertUserInputToTensorFlowFormat(userInput: UserInput): FloatArray {
        // Convert userInput to FloatArray as shown in previous examples
        return FloatArray(1) { 0.0f }
    }*/
}